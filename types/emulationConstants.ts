import type { WindowSize } from "./lhparams";
// We can't import type so grab it from the /lib/types to avoid pulling in more than we need
export const windowSizeDesktop: WindowSize = { width: 1920, height: 1080 };
